{
  "name": "kana-master",
  "version": "1.0.0",
  "description": "히라가나·가타가나 완전 학습앱 — Hiragana & Katakana master PWA",
  "private": true,
  "scripts": {
    "dev": "npx serve . -p 3000 --no-clipboard",
    "lint": "node --check js/v2/app.js && node --check js/v2/content-index.js && node --check js/v2/curriculum.js && node --check js/v2/store.js && node --check js/v2/tts.js && node --check js/v2/utils.js",
    "size": "node scripts/check-size.js",
    "zip": "node scripts/build-zip.js",
    "check": "npm run lint && npm run size"
  },
  "devDependencies": {
    "serve": "^14.2.3",
    "archiver": "^7.0.1"
  }
}
